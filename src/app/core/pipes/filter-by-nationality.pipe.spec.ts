import { FilterByNationalityPipe } from './filter-by-nationality.pipe';

describe('FilterByNationalityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNationalityPipe();
    expect(pipe).toBeTruthy();
  });
});
